# Even or Odd in Pile programming language
import "std/io.pile"

proc EvenOdd
    let n
    n 2 % 0 = if
        "even"
    else
        "odd"
    end
end

STDOUT "Number input: " write
Input toint EvenOdd let choice

"The number is " choice + Print