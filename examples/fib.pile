proc fib
    let n
    0 1 as a b let
        loop
            b println

            a b + as temp let
                b let a
                temp let b
            end
            
            n b > if stop end
        end
    end
end

60000 fib