proc fib
    let n
    0 1 as a b let
        loop
            b println
            a b + let temp
            b let a
            temp let b
            n b > if break end
        end
    end
end

60000 fib